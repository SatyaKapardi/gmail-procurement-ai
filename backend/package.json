{
  "name": "gmail-procurement-ai-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for Gmail Procurement AI",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute procurement-db --file=./schema.sql"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
